# SQLite database (default: backend/data/freshsave.db)
# DATABASE_URL=sqlite:///./data/freshsave.db

# JWT — change in production
JWT_SECRET_KEY=your-long-random-secret-here
ACCESS_TOKEN_EXPIRE_DAYS=7
